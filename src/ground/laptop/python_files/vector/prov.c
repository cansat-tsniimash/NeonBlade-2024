#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdint.h>
#include <math.h> 
#include "main.c"

int main() 
{
	float q[4] = {sqrt(1)/2, 0, 0, sqrt(1) / 2 };
	float gps[4] = 0;
	double height = 10.0m l;
	int time = 1000;
	double vector[3] = {0, 0, 1};
	kakashka(q, gps, height, l, time, vector);
}